/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "developers.google.com",
      "lh3.googleusercontent.com",
    ],
  },
};

export default nextConfig;
